package model;

public class Ordenamiento {
    public static Libro[] quickSortCodigo(Libro[] libros) {
        int n = libros.length;
        Libro[] temp = libros.clone();

        return temp;
    }
}
